.tasks.edit-page
  .card
    .card-body
      %h2 Editando tarea

      = render 'form'

      = link_to 'show', @task, class: 'btn-interaction'
      \|
      = link_to 'back', tasks_path, class: 'btn-interaction'