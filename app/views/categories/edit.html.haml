.category.edit-page
	.card
		.card-body
			%h2 Editando categoría
			
			= render 'form'
			= link_to 'Show', @category, class: 'btn-interaction'
			\|
			= link_to 'Back', categories_path, class: 'btn-interaction'